# nihonium
A Selenium-API-compatible automation engine built directly on Chrome DevTools Protocol (CDP). It reimplements Selenium’s internal execution model without ChromeDriver or HTTP, providing Playwright-style auto-wait, resilient locator-based interactions, and faster, more stable browser automation — all without changing existing test code
